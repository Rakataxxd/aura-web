node_modules
dist
*.local
.DS_Store

# video de prueba: solo para verificar deteccion en local, no se publica
public/testdata

# worker de Cloudflare
worker/.wrangler
worker/node_modules
# secretos locales del worker (ADMIN_CLAVE, SAL_VISITANTE). En produccion van
# con `wrangler secret put`, nunca en el repo.
worker/.dev.vars

# override local del backend (.env SI se versiona: la URL no es secreta
# y sin ella el build sale sin ranking sin avisar)
.env.*.local

# scratch de diagnostico (mosaicos de frames)
.tmp
dist-local/
