# Build del modo de emergencia: el sitio servido desde esta PC por el tunel
# (ver dev/servidor-local.mjs). Lo carga `npm run build:local`.
#
# Le pega a su MISMO origen a proposito: asi `dev/servidor-local.mjs` puede
# partir el trafico —cola y salas al wrangler de aca, ranking al worker de
# produccion— sin CORS de por medio y sin `?api=` en la URL.
VITE_API_RANKING=https://casa.auratester.com
